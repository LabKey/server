# Properties that tend to change between machines and system instances

# Just map the network drive on a dev machine.
networkDriveLetter=t
networkDrivePath=\\\\fred\\stedi
networkDriveUser=none
networkDrivePassword=none

smtpHost=localhost
smtpUser=Anonymous
smtpPort=25

# Use the jTDS jdbc driver on localhost
jdbcDriverClassName=net.sourceforge.jtds.jdbc.Driver
jdbcURL=jdbc:jtds:sqlserver://localhost/labkey
jdbcUser=sa
jdbcPassword=sa
